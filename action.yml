name: "Fortune Commenter"
description: "Add a fortune message as a comment on new pull requests"
branding:
  icon: "message-circle"
  color: "purple"
inputs: {}
outputs: {}
runs:
  using: "docker"
  image: "Dockerfile"  
